This is a simple application using the eBaySF UCM and the Joomla 12.1 mvc.
If you want to use it you need to merge the pluatform and the files from the eBaySF UCM repo.
It has two themes, beez and simplecontent (which uses the Bootstrap framework). You change
themes in the configuration.php file.
